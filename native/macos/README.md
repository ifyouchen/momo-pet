# Native macOS

macOS-specific native capability notes and adapters.

## Owns

- macOS menu bar behavior.
- AppKit window level edge cases.
- Space, full-screen, signing, and notarization notes.

Implementation should remain behind the Tauri runtime boundary.
